import React from 'react';

import {Text, View} from 'react-native';

const AppointmentCreate: React.FC = () => {
  return (
    <Text>AppointmentCreate</Text>
  );
};

export default AppointmentCreate;
